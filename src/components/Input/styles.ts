import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 52,
    paddingHorizontal: 20,
    fontSize: 16,
    borderColor: "#77dd77",
    borderWidth: 3,
    borderRadius: 8,
  },
});
